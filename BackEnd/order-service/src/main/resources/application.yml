server:
  port: 8085

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/farmconnect_order?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  mail:
    host: smtp.gmail.com
    port: 587
    username: farmconnect4you@gmail.com
    password: lkfz nuip pejt pijk
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

farmconnect:
  notifications:
    admin-email: farmconnect4you@gmail.com
  payment:
    razorpay-link: https://razorpay.me/@gunduvenkatanarasimhulu
    razorpay-key-id: ${RAZORPAY_KEY_ID:${razorpay.key.id:}}
    razorpay-key-secret: ${RAZORPAY_KEY_SECRET:${razorpay.key.secret:}}
    razorpay-checkout-name: FarmConnect
    razorpay-checkout-description: FarmConnect Order Payment
